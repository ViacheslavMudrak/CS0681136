"use client";

/**
 * Orders tab toolbar — re-exports the shared implementation from `partial/`
 * so tab-specific entry points stay under `tabs/orders/`.
 */
export { OrderManagementToolbar as OrdersManagementToolbar } from "../../partial/OrderManagementToolbar";
