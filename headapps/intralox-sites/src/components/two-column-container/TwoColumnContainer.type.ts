import type { ComponentProps } from 'lib/component-props';

/**
 * Layout-only rendering: `Size` drives column ratios; child renderings live in
 * `left-column-{*}` and `right-column-{*}` placeholders.
 */
export type TwoColumnContainerProps = ComponentProps;
