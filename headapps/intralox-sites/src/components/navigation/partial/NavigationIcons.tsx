/**
 * Header chrome icons — implementation lives in {@link lib/chrome-icons}.
 * Re-exported here so existing navigation imports stay stable.
 */
export {
  ChromeIconFromCms,
  CmsIcon,
  FaIconFromCms,
  HEADER_ICON_DEFAULTS,
  ICONS,
  isFontAwesomeClassString,
  normalizeFontAwesomeClassList,
  UI_ICONS,
} from 'lib/chrome-icons';
