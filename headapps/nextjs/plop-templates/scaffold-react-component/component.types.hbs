import { Field, ImageField, LinkField } from '@sitecore-content-sdk/nextjs';
import { ComponentProps } from 'lib/component-props';
// import { ResponsiveImageFields } from 'models/responsive-image';

type Sample{{pascalCase name}}Fields = {
  sampleTitle: Field<string>;
  sampleBody: Field<string>;
  sampleLink: LinkField;
  sampleLinkText: Field<string>;
  sampleImage: ImageField;
};

export type {{pascalCase name}}Props = ComponentProps & {
  fields: Sample{{pascalCase name}}Fields;
};
